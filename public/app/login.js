import React from 'react';
import ReactDOM from 'react-dom';
import {Form,Input,Button} from 'element-react';
import 'element-theme-default';
import LogForm from './logTheme';


ReactDOM.render(
    <LogForm></LogForm>,
    document.getElementById('main')
);